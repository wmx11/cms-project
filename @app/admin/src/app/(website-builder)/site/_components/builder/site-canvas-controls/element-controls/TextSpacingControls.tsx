'use client';
import useStyles from '@admin/hooks/useStyles';

const TextSpacingControls = () => {
  const { applyStyles, getActiveStyles } = useStyles();

  // Text wrap
  // White space
  // Word spacing
  // Letter spacing
  // Word break

  return <div>TextSpacingControls</div>;
};

export default TextSpacingControls;
