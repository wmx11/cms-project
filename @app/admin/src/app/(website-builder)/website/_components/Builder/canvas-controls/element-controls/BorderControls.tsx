'use client';
import useStyles from '@admin/hooks/useStyles';

const BorderControls = () => {
  const { applyStyles, getActiveStyles } = useStyles();

  // Border width
  // Border color
  // Border type
  // Border position
  // Border radius

  return <div>BorderControls</div>;
};

export default BorderControls;
