'use client';
import useStyles from '@admin/hooks/useStyles';

const TransformControls = () => {
  const { applyStyles, getActiveStyles } = useStyles();

  // Rotate
  // Translate Y X Z

  return <div>TransformControls</div>;
};

export default TransformControls;
